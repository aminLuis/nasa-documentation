import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'documentation-nasa',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
